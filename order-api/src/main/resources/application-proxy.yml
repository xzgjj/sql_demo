spring:
  datasource:
    url: jdbc:mysql://${MINIDB_PROXY_HOST:127.0.0.1}:${MINIDB_PROXY_PORT:3306}/${MINIDB_PRIMARY_DATABASE:minidb}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai&characterEncoding=utf8mb4&useServerPrepStmts=false
    username: ${MINIDB_PROXY_USERNAME:proxy}
    password: ${MINIDB_PROXY_PASSWORD:proxy123}

minidb:
  order:
    proxy-mode: true
